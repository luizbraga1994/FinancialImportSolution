@model FinancialImport.Web.Models.DashboardViewModel
@{
    ViewData["Title"] = "Dashboard";
}

<h2>Dashboard</h2>

<div class="card">
    <div class="card-body">
        <p><strong>Usuário:</strong> @Model.UserName</p>
        <p><strong>Company:</strong> @(Model.CompanyName ?? Model.CompanyDb ?? "Não selecionada")</p>
    </div>
</div>
